# Validated Persistent Signup Form

A small signup form that validates names and Ethiopian phone numbers, then saves valid entries to localStorage as JSON.

It also remembers the selected theme and restores it when the page is loaded.

## How to open

Open `index.html` in a web browser.